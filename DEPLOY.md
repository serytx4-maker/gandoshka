# 🚀 Деплой на Vercel

## Шаг 1: Загрузи на GitHub

1. Создай репозиторий на [github.com](https://github.com/new)
   - Название: `music-streaming-app` (или любое)
   - Сделай **Private** или **Public** - как хочешь
   - НЕ добавляй README, .gitignore (они уже есть)

2. В терминале в папке проекта выполни:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/твой-username/music-streaming-app.git
git push -u origin main
```

(Замени `твой-username` на свой GitHub username)

---

## Шаг 2: Деплой на Vercel

1. Открой [vercel.com](https://vercel.com)
2. Нажми **"Sign Up"** или **"Log In"** (лучше через GitHub)
3. Нажми **"Add New..."** → **"Project"**
4. Найди свой репозиторий `music-streaming-app`
5. Нажми **"Import"**

---

## Шаг 3: Настрой переменные окружения

На странице настройки проекта:

1. Разверни секцию **"Environment Variables"**
2. Добавь две переменные:

**Переменная 1:**
- Name: `NEXT_PUBLIC_SUPABASE_URL`
- Value: `https://твой-проект.supabase.co` (из Supabase Settings → API)

**Переменная 2:**
- Name: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- Value: `твой_anon_ключ` (из Supabase Settings → API)

3. Нажми **"Deploy"**

---

## Шаг 4: Подожди 2 минуты

Vercel соберет и задеплоит проект. Увидишь:
- 🔨 Building...
- ✅ Deployed!

Получишь URL типа: `https://music-streaming-app-xxx.vercel.app`

---

## Шаг 5: Настрой Supabase для продакшена

1. Открой Supabase → **Authentication** → **URL Configuration**
2. В **Site URL** добавь свой Vercel URL: `https://твой-проект.vercel.app`
3. В **Redirect URLs** добавь: `https://твой-проект.vercel.app/**`
4. Сохрани

---

## 🎉 Готово!

Твой сайт онлайн! Открой URL и проверь:
- ✅ Регистрация работает
- ✅ Загрузка треков работает
- ✅ Плеер работает

---

## 📝 Обновление сайта

Когда захочешь обновить код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит новую версию!

---

## 🐛 Если что-то не работает

### Ошибка "Invalid API key"
- Проверь переменные окружения в Vercel
- Settings → Environment Variables
- Убедись что нет лишних пробелов

### Ошибка при регистрации
- Проверь Site URL в Supabase
- Должен совпадать с Vercel URL

### Не загружаются файлы
- Проверь что buckets `audio` и `covers` публичные
- Проверь политики Storage в Supabase

---

## 💡 Полезные команды

```bash
# Проверить статус git
git status

# Посмотреть изменения
git diff

# Отменить изменения
git checkout .

# Создать новую ветку
git checkout -b feature-name
```

---

Всё! Теперь у тебя свой музыкальный стриминг онлайн 🎵
