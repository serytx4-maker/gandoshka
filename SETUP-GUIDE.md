# 🚀 Пошаговая настройка проекта

## Шаг 1: Создание проекта в Supabase

1. Откройте [supabase.com](https://supabase.com)
2. Нажмите **"New project"**
3. Заполните:
   - **Name**: music-streaming (или любое название)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
4. Нажмите **"Create new project"**
5. Подождите 1-2 минуты пока проект создается

---

## Шаг 2: Создание таблиц в базе данных

1. В левом меню нажмите **"SQL Editor"**
2. Нажмите **"New query"**
3. Скопируйте ВЕСЬ код из файла `supabase-setup.sql` (он в корне проекта)
4. Вставьте в редактор SQL
5. Нажмите **"Run"** (или Ctrl+Enter)
6. Должно появиться сообщение "Success. No rows returned"

✅ Таблицы созданы!

---

## Шаг 3: Создание Storage для файлов

### 3.1 Создание bucket для аудио

1. В левом меню нажмите **"Storage"**
2. Нажмите **"Create a new bucket"**
3. Заполните:
   - **Name**: `audio`
   - **Public bucket**: ✅ ВКЛЮЧИТЕ (галочка должна стоять!)
4. Нажмите **"Create bucket"**

### 3.2 Создание bucket для обложек

1. Снова нажмите **"Create a new bucket"**
2. Заполните:
   - **Name**: `covers`
   - **Public bucket**: ✅ ВКЛЮЧИТЕ
3. Нажмите **"Create bucket"**

✅ Storage настроен!

---

## Шаг 4: Настройка Google OAuth (ОПЦИОНАЛЬНО - можно пропустить!)

> ⚠️ **Этот шаг можно пропустить!** Вход через Email/Password будет работать без Google OAuth.
> Настраивайте Google только если хотите кнопку "Войти через Google".

### Простой способ настроить Google OAuth:

1. **Откройте Google Cloud Console**: [console.cloud.google.com](https://console.cloud.google.com)

2. **Создайте проект** (если нет):
   - Вверху нажмите на название проекта → "New Project"
   - Название: `music-streaming` → Create

3. **Найдите "APIs & Services"** в левом меню (☰ гамбургер-меню)
   - Если не видите меню - нажмите на ☰ в левом верхнем углу

4. **OAuth consent screen**:
   - Нажмите "OAuth consent screen" в левом меню
   - Выберите "External" → Create
   - Заполните только обязательные поля:
     - App name: `Music Streaming`
     - User support email: ваш email
     - Developer contact: ваш email
   - Жмите "Save and Continue" → "Save and Continue" → "Save and Continue"

5. **Credentials** (ГДЕ НАХОДЯТСЯ CREDENTIALS):
   - В левом меню нажмите **"Credentials"** (это и есть то что ты искал!)
   - Нажмите "+ CREATE CREDENTIALS" вверху
   - Выберите "OAuth client ID"
   - Application type: "Web application"
   - Name: `Music Streaming`
   
6. **Authorized redirect URIs** (ВАЖНО!):
   - Откройте Supabase в другой вкладке
   - Перейдите: Authentication → Providers → Google
   - Скопируйте "Callback URL" (например: `https://abcdefgh.supabase.co/auth/v1/callback`)
   - Вернитесь в Google Cloud
   - В "Authorized redirect URIs" нажмите "ADD URI"
   - Вставьте скопированный URL
   - Нажмите "CREATE"

7. **Скопируйте ключи**:
   - Появится окно с Client ID и Client Secret
   - **Client ID** - длинная строка, заканчивается на `.apps.googleusercontent.com`
   - **Client Secret** - короткая строка
   - Скопируйте оба!

8. **Вставьте в Supabase**:
   - Supabase → Authentication → Providers → Google
   - Включите переключатель "Enable Sign in with Google"
   - Вставьте Client ID и Client Secret
   - Save

✅ Google OAuth настроен!

---

### ❌ Если не получается - просто пропустите этот шаг!

Вход через Email/Password работает отлично без Google OAuth.

---

## Шаг 5: Получение ключей Supabase

1. В Supabase перейдите в **"Settings"** (шестеренка внизу слева)
2. Нажмите **"API"**
3. Найдите и скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка, начинается с `eyJ...`)

---

## Шаг 6: Настройка переменных окружения

1. В корне проекта создайте файл `.env.local` (если его нет)
2. Вставьте туда:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_ключ
```

**Пример:**
```env
NEXT_PUBLIC_SUPABASE_URL=https://abcdefgh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

✅ Переменные настроены!

---

## Шаг 7: Установка зависимостей и запуск

1. Откройте терминал в папке проекта
2. Установите зависимости:

```bash
npm install
```

3. Запустите проект:

```bash
npm run dev
```

4. Откройте браузер: [http://localhost:3000](http://localhost:3000)

✅ Проект запущен!

---

## Шаг 8: Тестирование

1. Нажмите **"Войти"** в сайдбаре
2. Попробуйте:
   - Зарегистрироваться через Email/Password
   - Войти через Google
3. После входа попробуйте загрузить трек через **"Загрузить"**

---

## 🎉 Готово!

Теперь у вас полностью рабочий музыкальный стриминг-сервис!

---

## 📝 Частые проблемы

### Ошибка "Invalid API key"
- Проверьте, что скопировали правильные ключи из Supabase
- Убедитесь, что в `.env.local` нет лишних пробелов
- Перезапустите сервер (`npm run dev`)

### Google OAuth не работает
- Проверьте, что Callback URL в Google Cloud точно совпадает с URL из Supabase
- Убедитесь, что включили Google Provider в Supabase
- Проверьте, что Client ID и Secret правильно скопированы

### Не загружаются файлы
- Убедитесь, что buckets `audio` и `covers` созданы
- Проверьте, что они помечены как **Public**
- В Storage → bucket → нажмите на три точки → Policies → убедитесь что есть публичный доступ

### База данных пустая
- Убедитесь, что выполнили SQL из `supabase-setup.sql`
- Проверьте в Table Editor, что таблицы созданы

---

## 🚀 Деплой на Vercel (опционально)

1. Загрузите проект на GitHub
2. Откройте [vercel.com](https://vercel.com)
3. Нажмите **"Import Project"**
4. Выберите ваш репозиторий
5. Добавьте переменные окружения:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
6. Нажмите **"Deploy"**
7. После деплоя добавьте URL вашего сайта в Google Cloud Console в Authorized redirect URIs

---

Если что-то не получается - пишите, разберемся! 🎵
